{{- if .Values.webhook.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "raps.fullname" . }}-webhook
  namespace: {{ include "raps.namespace" . }}
  labels:
    {{- include "raps.labels" . | nindent 4 }}
    app.kubernetes.io/component: webhook
  {{- with .Values.webhook.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.webhook.ingress.className }}
  {{- if .Values.webhook.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.webhook.ingress.host }}
      secretName: {{ .Values.webhook.ingress.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ .Values.webhook.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "raps.fullname" . }}-webhook
                port:
                  name: http
{{- end }}
